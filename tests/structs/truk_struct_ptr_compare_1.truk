struct Point {
  x: i32,
  y: i32
}

fn main() : i32 {
  var ptr1: *Point = alloc(@Point);
  var ptr2: *Point = alloc(@Point);
  
  if ptr1 != ptr2 {
    free(ptr1);
    free(ptr2);
    return 1;
  }
  
  free(ptr1);
  free(ptr2);
  return 0;
}
