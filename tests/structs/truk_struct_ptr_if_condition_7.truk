struct Point {
  x: i32,
  y: i32
}

fn main() : i32 {
  var ptr: *Point = alloc(@Point);
  (*ptr).x = 7;
  (*ptr).y = 3;
  
  var result: i32 = 0;
  if ptr != nil {
    result = (*ptr).x;
  }
  
  free(ptr);
  return result;
}
