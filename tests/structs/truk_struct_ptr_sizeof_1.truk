struct Point {
  x: i32,
  y: i32
}

fn main() : i32 {
  var ptr_size: u64 = sizeof(@*Point);
  var struct_size: u64 = sizeof(@Point);
  
  if ptr_size <= struct_size {
    return 1;
  }
  return 0;
}
