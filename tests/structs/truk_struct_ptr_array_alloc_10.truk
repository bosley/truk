struct Point {
  x: i32,
  y: i32
}

fn main() : i32 {
  var count: u64 = 3;
  var arr: []*Point = make(@*Point, count);
  
  arr[0] = make(@Point);
  (*arr[0]).x = 10;
  (*arr[0]).y = 5;
  
  var result: i32 = (*arr[0]).x;
  
  delete(arr[0]);
  delete(arr);
  return result;
}
