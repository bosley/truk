struct Inner {
  value: i32
}

struct Outer {
  inner: Inner,
  extra: i32
}

fn main() : i32 {
  var outer: Outer;
  outer.inner.value = 30;
  outer.extra = 12;
  return outer.inner.value + outer.extra;
}
