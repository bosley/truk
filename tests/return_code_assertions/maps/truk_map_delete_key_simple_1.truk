fn main() : i32 {
  var m: map[*u8, i32] = make(@map[*u8, i32]);
  
  m["key"] = 10;
  
  delete(m["key"]);
  
  var ptr: *i32 = m["key"];
  if ptr == nil {
    delete(m);
    return 1;
  }
  
  delete(m);
  return 0;
}
