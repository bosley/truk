struct Box<T> {
  value: T
}

fn main() : i32 {
  var inner: Box<i32> = Box<i32>{value: 88};
  var outer: Box<Box<i32>> = Box<Box<i32>>{value: inner};
  var ptr: *Box<Box<i32>> = &outer;
  
  return (*ptr).value.value;
}
