struct Box<T> {
  value: T
}

fn apply(b: Box<i32>, f: fn(i32) : i32) : i32 {
  return f(b.value);
}

fn main() : i32 {
  var b: Box<i32> = Box<i32>{value: 10};
  
  var doubler: fn(i32) : i32 = fn(x: i32) : i32 {
    return x * 2;
  };
  
  return apply(b, doubler);
}
