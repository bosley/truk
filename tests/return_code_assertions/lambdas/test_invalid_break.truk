fn main() : i32 {
  var count: u64 = 5;
  var slice: []i32 = make(@i32, count);
  
  var sum: i32 = 0;
  each(slice, &sum, fn(elem: *i32, ctx: *i32) : bool {
    break;
    return true;
  });
  
  delete(slice);
  return sum;
}
