cimport <stdio.h>;

extern fn printf(fmt: *i8, ...args): i32;

fn mixed_i32_ptr(count: i32, ...values) : i32 {
  var a: i32 = __TRUK_VA_ARG_I32();
  var ptr: *i32 = __TRUK_VA_ARG_PTR() as *i32;
  var b: i32 = *ptr;
  printf("a(i32)=%d b(from ptr)=%d\n", a, b);
  return a + b;
}

fn main() : i32 {
  var x: i32 = 20;
  return mixed_i32_ptr(2, 17, &x);
}
