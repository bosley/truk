var global: i32 = 0;

fn maybe_increment(flag: bool) : void {
  if flag {
    global = global + 5;
    return;
  }
  global = global + 10;
}

fn main() : i32 {
  maybe_increment(true);
  return global;
}
