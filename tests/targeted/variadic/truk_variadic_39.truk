fn mixed_conditional(mode: i32, ...values) : i32 {
  if mode == 1 {
    var a: i32 = __TRUK_VA_ARG_I32();
    var b: i64 = __TRUK_VA_ARG_I64();
    printf("Mode 1: a(i32)=%d b(i64)=%lld\n", a, b);
    return a + b as i32;
  } else {
    var x: f64 = __TRUK_VA_ARG_F64();
    printf("Mode 2: x(f64)=%f\n", x);
    return x as i32;
  }
}

fn main() : i32 {
  return mixed_conditional(2, 39.0);
}
