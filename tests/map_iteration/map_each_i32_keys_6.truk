fn main() : i32 {
  var m: map[i32, i32] = make(@map[i32, i32]);
  m[1] = 1;
  m[2] = 2;
  m[3] = 3;
  
  var sum: i32 = 0;
  each(m, &sum, fn(key: i32, val: *i32, ctx: *i32) : bool {
    if val != nil {
      *ctx = *ctx + *val;
    }
    return true;
  });
  
  delete(m);
  return sum;
}
