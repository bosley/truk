fn main() : i32 {
  var m: map[*u8, i32] = make(@map[*u8, i32]);
  
  var count: i32 = 0;
  each(m, &count, fn(key: *u8, val: *i32, ctx: *i32) : bool {
    *ctx = *ctx + 1;
    return true;
  });
  
  delete(m);
  return count;
}