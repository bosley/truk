cimport <stdio.h>;

extern fn printf(fmt: *i8, ...args): i32;

fn mixed_i32_i64(count: i32, ...values) : i32 {
  var a: i32 = __TRUK_VA_ARG_I32();
  var b: i64 = __TRUK_VA_ARG_I64();
  printf("a(i32)=%d b(i64)=%lld\n", a, b);
  return (a as i64 + b) as i32;
}

fn main() : i32 {
  return mixed_i32_i64(2, 10, 23);
}
