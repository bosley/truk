fn get_lambda() : fn(i32) : (i32, i32) {
  return fn(x: i32) : (i32, i32) {
    return x, x * 2;
  };
}

fn main() : i32 {
  var func: fn(i32) : (i32, i32) = get_lambda();
  let a, b = func(10);
  return a + b - 10;
}
