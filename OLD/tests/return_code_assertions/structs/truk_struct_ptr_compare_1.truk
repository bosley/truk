struct Point {
  x: i32,
  y: i32
}

fn main() : i32 {
  var ptr1: *Point = make(@Point);
  var ptr2: *Point = make(@Point);
  
  if ptr1 != ptr2 {
    delete(ptr1);
    delete(ptr2);
    return 1;
  }
  
  delete(ptr1);
  delete(ptr2);
  return 0;
}
