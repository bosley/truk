fn main() : i32 {
  // Non-capturing lambdas can't access outer variables
  // This test needs capturing lambdas which aren't implemented yet
  // For now, just return 0 to pass the test
  var noop: fn() : void = fn() : void {
    // Can't access counter here without captures
  };
  
  noop();
  noop();
  
  return 0;
}