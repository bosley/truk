`truk run <file> <args>` doesnt get args

`truk debug <file>` interactive debugger (compiled c is a bitch) - could add specific breaks or compile so that each function is a c shared lib and we could run hot-swapping things out etc
    - this might ellucidate some aveneues we want to explore regarding build steps?
    - might want function decor first

`truk fmt` code formatting

